<?xml version="1.0" ?>
<!--
  Gazebo Harmonic GUI configuration for MG6010 robot simulation
  Includes ViewAngle plugin for quick camera preset switching
-->
<gui fullscreen="0">

  <!-- 3D scene -->
  <plugin filename="MinimalScene" name="3D View">
    <gz-gui>
      <title>3D View</title>
      <property type="bool" key="showTitleBar">false</property>
      <property type="string" key="state">docked</property>
    </gz-gui>

    <engine>ogre2</engine>
    <scene>scene</scene>
    <ambient_light>0.4 0.4 0.4</ambient_light>
    <background_color>0.8 0.8 0.8</background_color>

    <!-- Initial camera: isometric view looking at the arm -->
    <camera_pose>1.8 1.8 1.5 0 0.4 -2.35</camera_pose>
  </plugin>

  <!-- Scene manager (required for rendering) -->
  <plugin filename="GzSceneManager" name="Scene Manager">
    <gz-gui>
      <property type="bool" key="showTitleBar">false</property>
      <property type="string" key="state">floating</property>
      <property type="double" key="height">0</property>
      <property type="double" key="width">0</property>
    </gz-gui>
  </plugin>

  <!-- Interactive mouse controls (orbit, pan, zoom) -->
  <plugin filename="InteractiveViewControl" name="Interactive View Control">
    <gz-gui>
      <property type="bool" key="showTitleBar">false</property>
    </gz-gui>
  </plugin>

  <!-- World control (play/pause/step) -->
  <plugin filename="WorldControl" name="World control">
    <gz-gui>
      <title>World control</title>
      <property type="bool" key="showTitleBar">false</property>
      <property type="bool" key="resizable">false</property>
      <property type="double" key="height">72</property>
      <property type="double" key="width">121</property>
      <property type="double" key="z">1</property>
      <property type="string" key="state">floating</property>
      <anchors target="3D View">
        <line own="left" target="left"/>
        <line own="bottom" target="bottom"/>
      </anchors>
    </gz-gui>
    <play_pause>true</play_pause>
    <step>true</step>
    <start_paused>false</start_paused>
  </plugin>

  <!-- World stats (sim time, real time, RTF) -->
  <plugin filename="WorldStats" name="World stats">
    <gz-gui>
      <title>World stats</title>
      <property type="bool" key="showTitleBar">false</property>
      <property type="bool" key="resizable">false</property>
      <property type="double" key="height">110</property>
      <property type="double" key="width">290</property>
      <property type="double" key="z">1</property>
      <property type="string" key="state">floating</property>
      <anchors target="3D View">
        <line own="right" target="right"/>
        <line own="bottom" target="bottom"/>
      </anchors>
    </gz-gui>
    <sim_time>true</sim_time>
    <real_time>true</real_time>
    <real_time_factor>true</real_time_factor>
    <iterations>true</iterations>
  </plugin>

  <!-- Entity tree (see all links/joints in sidebar) -->
  <plugin filename="EntityTree" name="Entity tree">
    <gz-gui>
      <property type="string" key="state">docked</property>
    </gz-gui>
  </plugin>

  <!-- Component inspector (click any link to see its properties) -->
  <plugin filename="ComponentInspector" name="Component inspector">
    <gz-gui>
      <property type="string" key="state">docked</property>
    </gz-gui>
  </plugin>

  <!-- ★ Camera Tracking — enables /gui/move_to/pose service for programmatic camera control -->
  <plugin filename="CameraTracking" name="Camera Tracking">
    <gz-gui>
      <property type="bool" key="showTitleBar">false</property>
      <property type="string" key="state">floating</property>
      <property type="double" key="height">0</property>
      <property type="double" key="width">0</property>
    </gz-gui>
  </plugin>

  <!-- ★ View Angle plugin - gives Front/Back/Left/Right/Top/Bottom buttons in sidebar -->
  <plugin filename="ViewAngle" name="View Angle">
    <gz-gui>
      <property type="string" key="state">docked</property>
      <property type="double" key="height">300</property>
    </gz-gui>
    <!-- Look at the arm (base is at ~0.46m height) -->
    <target>0 0 0.5</target>
  </plugin>

  <!-- Transform control (translate/rotate/scale objects) -->
  <plugin filename="TransformControl" name="Transform control">
    <gz-gui>
      <property type="string" key="state">docked</property>
    </gz-gui>
  </plugin>

  <!-- Tape measure tool -->
  <plugin filename="TapeMeasure" name="Tape measure">
    <gz-gui>
      <property type="string" key="state">docked</property>
    </gz-gui>
  </plugin>

</gui>
